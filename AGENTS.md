# Ameme 协作与交付规则

本仓库同时承载产品研究、正式文档、客户端/服务端代码、连接器、基础设施、验证和发布材料。所有人和 AI 都必须以“可追溯、可验证、可回滚、可上线”为完成标准。

## 1. 开工阅读顺序

1. 读取本文件。
2. 读取 `WORKSPACE_MAP.md`，只定位任务入口，不扫描整个仓库。
3. 涉及当前状态时读取 `STATUS.md`；涉及任务执行时读取 `JOBS/README.md` 和对应 Job。
4. 进入文档域时先读该目录 `_INDEX.md`；存在 `_CURRENT.md` 时，以它指向的文件为当前正本。
5. 只读取当前任务需要的正文、源码、测试和证据；历史材料按需读取。

`_archive/`、`deprecated/`、`raw/`、生成物和旧版本默认不是事实来源。

## 2. 根目录边界

根目录只允许系统入口、许可证和配置文件：

- `README.md`、`AGENTS.md`、`WORKSPACE_MAP.md`、`STATUS.md`、`WAL.md`
- `.gitignore`、`.gitattributes`、`.editorconfig`、`.env.example`
- `JOBS/`、`docs/`、`research/`、`apps/`、`services/`、`packages/`、`connectors/`、`infra/`、`scripts/`、`tests/`、`.github/`、`.obsidian/`

临时脚本、截图、数据库、日志和模型输出不得散落在根目录。

## 3. 目录职责

| 目录 | 职责 |
|---|---|
| `JOBS/` | 工作队列、执行计划、进度、阻塞和确认门 |
| `docs/product/` | 用户、场景、定位、PRD、指标、商业模式、路线图 |
| `docs/research/` | 调研综合结论、来源、访谈和实验结论 |
| `docs/architecture/` | 系统、数据、模型、同步和技术架构 |
| `docs/privacy-security/` | 隐私模型、威胁模型、权限、合规、数据保留与删除 |
| `docs/engineering/` | RFC、接口、Schema、迁移、工程计划和开发手册 |
| `docs/quality/` | 测试策略、评测集、验证报告和发布质量证据 |
| `docs/release/` | 发布计划、Gate、版本说明、回滚和商店材料 |
| `docs/operations/` | 监控、告警、事故响应、客服和运行手册 |
| `docs/decisions/` | 架构/产品/安全决策记录及被替代关系 |
| `docs/governance/` | 工作区规范、工作流、Gate 和模板 |
| `research/` | 可复跑探针、技术 Spike 和脱敏实验材料 |
| `apps/` | 用户端应用：desktop、mobile、web、browser-extension |
| `services/` | 云端 API、异步处理、同步和账户服务 |
| `packages/` | 共享领域模型、存储、加密、UI 和 SDK |
| `connectors/` | 信息源适配器与 Connector SDK 实现 |
| `infra/` | 环境、部署、密钥引用、可观测性和发布基础设施 |
| `scripts/` | 治理、构建、迁移、验证和发布辅助脚本 |
| `tests/` | 跨组件测试、合成夹具、端到端测试和性能测试 |

## 4. 正本、索引和版本

- 每个正式文档域维护 `_INDEX.md`，只记录文件路由、状态和简短变更，不内嵌大段正文。
- 某一主题存在多个有效候选或历史版本时，建立 `_CURRENT.md` 指向唯一正本和阅读顺序。
- 更新正式文档时优先修改正本；新版本替代旧版时，同一变更中更新 `_CURRENT.md`、`_INDEX.md` 和旧版状态。
- 历史文件移入 `_archive/`，或在文件头标明“已被替代”和当前正本路径。
- `WORKSPACE_MAP.md` 只在一级目录、任务路由或跨目录正本入口变化时更新，禁止记录普通文件流水。
- 超过 30KB 的 Markdown 必须在文件头写明：文档状态、适合读者、人类快速阅读、AI 阅读提示。

## 5. Job 与状态机

预计步骤达到 5 步、跨多个目录、影响数据/架构/发布或需要用户确认的工作，必须建立 Job。

状态：

`proposed -> plan_ready -> approved -> in_progress -> review_ready -> accepted -> done`

旁路状态：`blocked`、`cancelled`。

- `JOBS/README.md` 只做短状态路由，不写详细流水。
- 详细目标、步骤、证据、阻塞、下一步和确认项写入 `JOBS/JOBS-Ameme.md`。
- 只有真实产物存在且验证命令实际通过，才能进入 `review_ready`。
- 用户/产品负责人确认 Gate 后进入 `accepted`；收尾和索引同步完成后进入 `done`。

用户介入三个时机：

1. 计划或范围确认。
2. 阻塞、不可逆操作、隐私边界或重大取舍。
3. Gate/发布确认。

日常执行、测试修复和文档同步不重复打断用户。

## 6. 产品到上线的证据 Gate

具体标准见 `docs/governance/产品生命周期与上线门禁.md`。任何 Gate 都不能用“文档已写完”替代验证证据。

- Gate 0：问题与愿景可讨论。
- Gate 1：用户、场景和数据源调研成立。
- Gate 2：Prototype 证明最小闭环可行。
- Gate 3：MVP 产品、架构、隐私、指标和成本可开工。
- Gate 4：工程功能完成并通过自动化质量门。
- Gate 5：封闭测试满足稳定性、信任和价值阈值。
- Gate 6：生产发布具备回滚、监控、支持、合规和负责人确认。
- Gate 7：发布后复盘并决定扩量、调整或停止。

## 7. 证据和验证

- 结论分为：已验证、部分验证、待验证、阻塞、否决。
- 测试报告必须包含命令、输入范围、输出摘要、时间、环境和已知限制。
- 禁止把计划中的文件、脚本、测试或指标写成“已完成”。
- 失败记录必须保留；不能为了得到通过结论而删除失败样本或静默修改验收标准。
- 标准执行顺序：生成/实现 → 证据审计 → 自动校验 → 预检 → 回归测试 → 索引同步 → 报告同步。
- 每次治理或正式交付变更后运行 `python scripts/governance/check_workspace.py`。

## 8. 数据与隐私硬规则

- 真实个人数据、浏览历史、聊天正文、照片、音频、精确位置、账单、健康数据、密钥和访问令牌不得提交到 Git。
- 测试默认使用 `tests/fixtures/synthetic/` 下的合成数据。
- 本地真实数据实验必须写明授权范围、空间、保留期和删除方式；原始数据放在被 `.gitignore` 排除的位置。
- 日志不得输出正文、密钥、完整 URL 查询参数或可识别个人的信息。
- 涉及云处理、跨端同步、第三方 Agent 访问或他人数据时，必须经过隐私与安全评审。
- 删除能力、数据 lineage 和访问审计属于核心功能，不得推迟到上线后补做。

## 9. 工程规范

- 技术栈确定后，在对应目录 README 和工程 RFC 中写清运行、测试、构建和发布命令。
- 公共领域对象和 Schema 放 `packages/`，不要在多个端复制不同定义。
- Connector 不得直写产品数据库，必须经过标准输入信封、权限和 lineage 层。
- 数据库 Schema 变更必须有迁移和回滚方案。
- 新增用户可见能力必须同时考虑指标、错误态、权限、删除和可观测性。
- 代码变更必须有与风险相称的单元、集成或端到端测试。

## 10. 文件写入规则

- 修改前先读目标目录 `_INDEX.md` 或 README。
- 新建/重大修改正式文档后，同一变更更新对应 `_INDEX.md`；正本变化时同步 `_CURRENT.md`。
- `scripts/` 和 `research/` 下的脚本前三行必须是用途、输入、输出。PowerShell 为兼容 Windows PowerShell 5.1 使用 ASCII `# Purpose / # Input / # Output`；Python 可使用同样英文或 `# 用途 / # 输入 / # 输出`。
- 面向跨团队阅读的文档避免只有内部人员理解的 Job 编号、缩写和代号；首次出现的行业缩写要解释。
- 不保留无价值的 `_v2_final_final` 文件；用文档内部版本和归档表达历史。

## 11. 发布权限

- 本地构建和验证可自动执行。
- 创建云资源、上传真实数据、推送生产配置、发布商店版本、开放外部测试或发送用户通知，必须获得用户明确确认。
- 每次生产发布必须有版本、负责人、变更范围、验证证据、监控指标、回滚条件和回滚步骤。
