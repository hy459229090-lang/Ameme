# P0 工作区基线复核（2026-08-08）

- Verdict：`conditional_pass`。仓库内静态/本地基线全部通过；外部不可替代门（真实参与者、物理设备、签名/发布、真实模型/付费服务、`production_recovery_claim`/`cross_device_recovery_claim`）仍为 `hold`。
- Scope/Build/Commit：`python3.12 -m venv /tmp/ameme-project-py312`；`/tmp/ameme-project-py312/bin/python -m pip install -r scripts/requirements-dev.txt`；`/tmp/ameme-project-py312/bin/python scripts/validation/run_workspace_validation.py`；实现/证据 head `8009f9ffbe0575d98cc0330689982110a4e2ef81`（Ameme）。文档同步提交只更新验收记录，不改变该实现输入。
- Gate/Spike ID：`workspace_validation_baseline`（仓库内合规模块 + 外部门包预检）。
- Owner/Reviewer/Date/Environment：Codex / 待产品/发布负责人 / 2026-08-08 / macOS (Command Line Tools) + Python 3.12 本地基线；GitHub Actions Xcode 26.6 / iOS 26.5 Simulator 与 API 36 / 16 KB AVD 远程设备基线。
- 隐私边界：仅使用合成/固定 fixtures、短时 pairing 缓存、公开仓库中的契约与测试数据；无真实用户正文/照片/音频/日历原文。

## 本轮复核目标与预注册标准

| 标准 | 判定 | 结果 |
|---|---|---|
| 合约与运行时静态基线 | 各合同/协议校验与单元测试通过 | pass |
| 引用与工作区治理 | Markdown 链接、工作区治理、Python 脚本入口通过 | pass |
| 外部门禁文档一致性 | 外部执行包可被调用且保真（保持 hold） | pass（hold） |
| Android 与 iOS 本地项目可构建前置校验 | installable/project + 共享扩展输入 + accessibility contract | pass |
| 外部硬 Gate（真实用户/签名/设备/模型/费用） | 只能在外部资源到位时才可关闭 | hold（保持待执行） |

## 环境与执行命令

- 环境：macOS、Python 3.12、项目根目录为本地仓库路径。
- 命令：
```bash
python3.12 -m venv /tmp/ameme-project-py312
/tmp/ameme-project-py312/bin/python -m pip install -r scripts/requirements-dev.txt
/tmp/ameme-project-py312/bin/python scripts/validation/run_workspace_validation.py
```

### 首次失败与重跑链

| 轮次 | 实际结果 | 处理 | 结论边界 |
|---|---|---|---|
| 直接使用未安装开发依赖的 Python 3.12 | 启动前依赖门 `python_dependencies` 按 `scripts/requirements-dev.txt` 检查到 `yaml`（`PyYAML`）、`cryptography`、`tzdata` 不完整，输出机器可读 JSON 后以退出码 2 结束 | 新建隔离 venv，并安装 `scripts/requirements-dev.txt` | 环境准备失败，未修改产品实现或验收标准 |
| 直接使用 macOS Command Line Tools Python 3.9 | 不满足 Sync 协议所需 Python 3.10+ 联合类型语法 | 使用上述隔离 Python 3.12；验证入口现会在启动前明确拒绝低版本解释器 | 环境不兼容，不代表 Sync/产品回归 |
| 直接使用 `python` 命令（非隔离） | 命令不存在：`zsh: command not found: python` | 切换到 `/tmp/ameme-project-py312-*` 并固定用 `python3.12` 执行 | 本轮结论不变；仅为本机别名差异，不影响脚本正确性 |
| 隔离 Python 3.12 + 开发依赖 | `run_workspace_validation.py` 28/28 通过 | 保留本报告的命令、输入与 Gate 输出 | 仅证明仓库内静态、合成与本地基线 |

## 结果摘要（已执行门）

| Gate | 指标 | 结论 |
|---|---|---|
| `contract_quality` | 2,715 compatibility + 2,709 contract checks | pass |
| `mobile_coverage_contract` | 280 checks | pass |
| `mobile_long_term_memory_contract` | 92 checks | pass |
| `mobile_recovery_contract` | 210 checks；`user_visible_same_install_recovery_claim=true` | pass |
| `mobile_reuse_contract` | 186 checks | pass |
| `mobile_source_deletion_contract` | 134 checks | pass |
| `mobile_field_provenance_contract` | 138 checks | pass |
| `mobile_user_confirmation_provenance_contract` | 94 checks | pass |
| `android_agent_revision_contract` | 87 checks | pass |
| `android_agent_undo_contract` | 117 checks | pass |
| `android_agent_read_contract` | 108 checks | pass |
| `android_agent_access_audit_contract` | 146 checks；`owner_visible_projection_claim=true` | pass |
| `ios_agent_local_node_client_contract` | 82 checks | pass |
| `mobile_local_space_deletion_contract` | 206 checks；本地安装收敛 claim=true | pass |
| `external_gate_packs` | 80 checks；`overall_verdict=hold` | hold（正常） |
| `core_reference` | 35 tests | pass |
| `sync_reference` | 7 gates + full unit set | pass |
| `agent_mock` | 全量通道测试通过 | pass |
| `agent_android_adapter` | 29 tests + mock adapter checks | pass |
| `ai_reference` | 38 tests | pass |
| `ai_fixed_eval` | 12 synthetic fixed eval cases，`case_count=12` | pass（`pass_synthetic_reference_regression`） |
| `skill_package` | 16 case / 15 risk / 7 files | pass |
| `ios_installable_project` | 52 checks | pass |
| `ios_share_extension_inputs` | 30 checks | pass |
| `ios_accessibility_contract` | 33 checks | pass |
| `pairing_qr_contract` | 62 checks，`server_enforced_one_time_secret=true` | pass |
| `markdown_links` | 45 links | pass |
| `workspace_governance` | 183 checks | pass |

最终结果：28/28 基线门（含 Python 与文档治理）通过，整体 `run_workspace_validation` 退出码 0，故本轮结论为 `conditional_pass`。新增纳入的 `mobile_user_confirmation_provenance_contract` 仅证明仓库内静态/合成合同边界，不改变外部门 verdict。

### 外部门执行包启动态准备复测（无敏感证据）

- 命令：
```bash
python3.12 scripts/validation/prepare_external_gate_run.py \
  --run-id demo-20260808b \
  --build-commit 8009f9ffbe0575d98cc0330689982110a4e2ef81

python3.12 scripts/validation/validate_external_gate_packs.py \
  data/private/external-gates/demo-20260808b/external-gate-summary-template.json
```
- 结果：
  - 本次重新执行 `prepare_external_gate_run.py` 成功（已使用新 run-id，避免复用既有 `demo-20260808`）；
  - 准备阶段生成 7 个模板文件与 manifest，`gate_claim=false`；
  - 输出目录：`data/private/external-gates/demo-20260808b`；
  - 验证阶段：`checks=80`，`overall_verdict=hold`，并明确 `real_participant_claim/physical_device_claim/signing_store_claim/production_recovery_claim/paid_provider_claim=false`。
- 结论：仓库内仅支持外部门“准备就绪/待真实执行”能力；未掩盖任何外部硬 Gate，且未采集真实参与者或设备数据。

## 失败与限制

| 限制 | 是否影响仓库内基线 | 处理 |
|---|---|---|
| 实际真实参与者路径未执行 | 是（`external_gate_packs.real_participant_claim=false`） | 继续保持外部执行包 hold |
| 物理双端设备与真实读屏未执行 | 是（`physical_device_claim=false`） | 需签名设备与发布负责人支持后执行 |
| 签名/商店/发布链路未执行 | 是（`signing_store_claim=false`） | 继续进入发布前 Gate |
| 跨设备生产恢复与卸载重建未闭环 | 是（`production_recovery_claim=false` / `cross_device_recovery_claim=false`） | 继续保留外部回归任务 |
| 真实模型/成本验证未执行 | 是（`real_model_quality_proven=false`） | 继续等待供应商与计量评估 |

## 结论与下一步

- 本轮可复跑结论：仓库内可执行 P0 静态与单元基线恢复到可接受状态，适合作为下一步外部 Gate 的启动证据。
- 下一步：在外部资源到位后继续执行 `docs/release/封闭Beta外部门执行包.md`、真实设备/读屏、共享 Grant 与账号设备服务、真实模型/成本验证。不得将本报告外推为真实发布或公开 Beta 通过。
