# P0 内部验证摘要（2026-08-08）

- Verdict：`conditional_pass`（仅仓库内验证）。外部门 verdict：`hold`。
- 输入：隔离 Python 3.12 环境、`scripts/requirements-dev.txt`、实现/证据 head `8009f9ffbe0575d98cc0330689982110a4e2ef81`。
- 边界：本摘要只汇总公开仓库中的静态、合成、单元与本地验证；不构成真实用户、物理设备、签名发布、真实模型、跨设备恢复或付费服务证据。

## 可复现命令

```bash
python3.12 -m venv /tmp/ameme-project-py312
/tmp/ameme-project-py312/bin/python -m pip install -r scripts/requirements-dev.txt
/tmp/ameme-project-py312/bin/python scripts/validation/run_workspace_validation.py
/tmp/ameme-project-py312/bin/python scripts/governance/check_workspace.py
```

## 内部结果

| 项目 | 结果 | 证据边界 |
|---|---|---|
| 工作区验证 | 28/28 Gate 通过，退出码 0 | 合同、参考运行时、固定 AI eval、iOS 项目静态前置、Markdown 与治理；不包含外部资源执行 |
| 工作区治理 | 183 checks，`errors=[]`、`warnings=[]` | 仓库结构与文档/契约治理 |
| 外部门执行包预检 | 80 checks，`overall_verdict=hold` | 只验证私有目录中的空白模板和证据格式，不采集真实数据 |
| 外部门执行包初始化 | `demo-20260808b`，`data/private/external-gates/demo-20260808b` | `gate_claim=false`、`evidence_status=blank_templates_only`；不代表 Gate 通过 |

## Claim 边界

| Claim | 当前值 | 含义 |
|---|---|---|
| `real_participant_claim` | `false` | 未执行真实参与者 Pilot |
| `physical_device_claim` | `false` | Simulator/AVD 不替代物理设备或真实读屏 |
| `signing_store_claim` | `false` | 未完成签名、内部分发或商店发布链路 |
| `production_recovery_claim` | `false` | 同安装恢复不等同生产、跨设备或设备丢失恢复 |
| `cross_device_recovery_claim` | `false` | 未证明跨设备密钥、卸载或换机恢复 |
| `paid_provider_claim` | `false` | 未验证真实付费供应商、成本或生产模型质量 |

## 环境失败链

未安装开发依赖的 Python 3.12 会在启动前由 `python_dependencies` 拒绝，并输出机器可读 JSON（缺失模块包括 `yaml`、`cryptography`、`tzdata`）；macOS Command Line Tools Python 3.9 会由 `python_runtime` 拒绝。两者均为环境准备失败，不代表产品回归。安装 `scripts/requirements-dev.txt` 后，上述 28/28 基线通过。

## 下一步

继续按 `../release/封闭Beta外部门执行包.md` 执行真实参与者、双端物理设备与读屏、签名/商店、真实 Agent/LAN、生产恢复和付费服务/成本 Gate。所有这些项目在取得相应私有证据并由负责人签收前保持 `hold`。
