# P0 上下文覆盖与长期记忆闭环验证

- Verdict：`conditional_pass`
- Scope/Build/Commit：未提交工作树；Coverage contract/Core reference/MCP Mock/Skill/研究执行包；不包含并行 App 视觉改动
- Gate/Spike ID：一天上下文覆盖与长期记忆 P0 / local-reference gate
- Owner/Reviewer：Codex 执行；产品负责人/安全/发布负责人待 Review
- Date/Environment：2026-07-26；macOS arm64；隔离 Python 3.12.13 venv

## Objective and preregistered criteria

| Criterion | Pass | Reject/stop | Source |
|---|---|---|---|
| 覆盖基线可复跑 | 目标分群、上下文、来源和合成日通过严格契约 | 缺字段权威/限制/硬门；输出虚假覆盖率或市场规模 | `packages/contracts/schemas/ameme-coverage.schema.json` |
| 保守编译 | 仅证据支持的信号形成候选；计划不升级；稀疏时间不制造事件 | 无来源对象仍产出事实；照片自动推断行为；弱推断升级 | `tests/core/test_coverage_compiler.py` |
| 删除语义 | Raw-only 保留结构化记录；source delete 沿 lineage 级联并可重建 | 删除后 Today/Recall 复活；两种用户意图混用 | `tests/core/test_raw_vault_and_queues.py` |
| 备份恢复 | 一致快照、hash/SQLite/Raw 校验、损坏拒绝、恢复到新目录后可读 | 只证明导出存在；损坏仍恢复；包含密钥；覆盖已有目标 | `tests/core/test_backup_recovery.py` |
| Agent 长期记忆边界 | Event/Revision 可在 exact Grant 内写入；敏感长期记忆和推断需确认 | 推断或偏好/关系/健康/财务/重大决定自动成为确认 Memory | `tests/agent/test_policy.py` |
| 后续复用 | 已验证 Event 可在重启后的任务中进入 ContextPack，并记录 useful feedback | 只写入、无法找回或没有 outcome | `tests/agent/test_core_store_integration.py` |
| 全链回归 | 统一工作区全部 Gate 通过 | 任一契约/Core/Sync/Agent/AI/Skill/文档/治理门失败 | `scripts/validation/run_workspace_validation.py` |

## Environment

- OS/device/runtime/version：macOS arm64；`python3.12 3.12.13`；临时 venv `/tmp/ameme-goal-py312-20260726`
- Account/feature flags/config：无账户、无生产密钥、无网络来源、无真实模型；只运行仓库 reference/mock/static gates
- Data class/fixture IDs：`synthetic_planning_baseline_not_market_evidence`；3 个合成 user-day；全部为 synthetic
- Network/power/background conditions：测试本身无网络；未运行 Android AVD/instrumentation、iOS Simulator 或物理设备
- 并行保护：未编辑、回退或格式化并行 session 的 `CommonComponents.kt`、`TodayScreen.kt`、`Theme.kt`、`AmemeApp.swift`

## Reproduction

```text
python3.12 -m venv /tmp/ameme-goal-py312-20260726
/tmp/ameme-goal-py312-20260726/bin/python -m pip install -r scripts/requirements-dev.txt
/tmp/ameme-goal-py312-20260726/bin/python scripts/validation/run_workspace_validation.py

PYTHONPATH=packages/core-reference python3 -m unittest discover -s tests/core -p "test_*.py" -v
PYTHONPATH=packages/core-reference:packages/agent-local-node-protocol:services/ameme-mcp-mock python3 -m unittest discover -s tests/agent -p "test_*.py" -v
PYTHONPATH=packages/core-reference:packages/agent-local-node-protocol:services/ameme-mcp-mock python3 scripts/validation/run_contract_quality.py
```

前三条是完整可复跑门禁。后面三条是本轮在系统 Python 3.9 环境中用于定向诊断的命令；Core/Agent/Contract 可通过，完整工作区的 Sync 包需要 Python 3.10+。

## Results

| Metric/assertion | Actual | Expected | Verdict | Evidence path |
|---|---:|---:|---|---|
| 统一工作区 Gate | 14/14 | 14/14 | pass | `scripts/validation/run_workspace_validation.py` |
| Coverage 合同检查 | 4,262，0 errors | 0 errors | pass | `scripts/validation/validate_coverage_contracts.py` |
| 分群 / 上下文 / 来源能力 | 5 / 10 / 10 | 预注册夹具 | pass_synthetic | `tests/fixtures/coverage/target-user-context-source-matrix-v1.json` |
| 合成 user-day / 观测 / 候选 / 缺口 | 3 / 8 / 5 / 6 | 预注册夹具 | pass_synthetic | `tests/fixtures/coverage/target-user-context-source-matrix-v1.json` |
| 市场规模状态 | `not_verified` | 不得伪造 | pass | `scripts/validation/validate_coverage_contracts.py` |
| Core reference | 46/46 tests | 全部通过 | pass | `tests/core/` |
| 其中 Coverage compiler | 9/9 tests | 全部通过 | pass | `tests/core/test_coverage_compiler.py` |
| 其中 Backup/restore | 4/4 tests | 全部通过 | pass_reference | `tests/core/test_backup_recovery.py` |
| Sync reference | 22/22 tests | 全部通过 | pass | `tests/sync/` |
| Agent mock | 35/35 tests | 全部通过 | pass | `tests/agent/` |
| Android Agent adapter reference | 20/20 tests | 全部通过 | pass | `services/ameme-mcp-mock/tests/` |
| AI reference | 38/38 tests；固定 Eval 12/12 | 全部通过 | pass_synthetic | `tests/ai/` |
| Skill package | 7 files；16 cases；15 risks | 0 errors | pass | `scripts/validation/validate_ameme_skill.py` |
| iOS 静态门 | project 44；share 30；accessibility 25 | 0 errors | pass_static | `scripts/validation/` |
| QR contract / Markdown / 治理 | 32 / 43 / 147 checks | 0 errors | pass | `scripts/validation/`、`scripts/governance/` |

本结果证明本地 reference 与研究工具已准备好，不能外推真实用户的一天覆盖、授权意愿、生产移动存储、灾难恢复、市场规模或公开发布。

## Failures and limitations

| Severity | Failure | Impact | Reproduce | Owner/action |
|---|---|---|---|---|
| setup | macOS 无 `python` 命令，仓库示例默认该入口 | 首次治理命令未运行 | `python scripts/governance/check_workspace.py` | 改用明确版本的 `python3`/venv；保留失败 |
| setup/fail-closed | 首次 Raw/backup 测试环境未安装 `cryptography` | 加密路径按设计失败关闭，无明文 fallback | 在无依赖环境运行 `tests/core/test_backup_recovery.py` | 安装锁定 `cryptography==46.0.7` 后复跑 |
| command | 直接按模块名运行非 package 测试路径失败 | 仅测试发现方式错误 | `PYTHONPATH=packages/core-reference python3 -m unittest tests.core.test_backup_recovery -v` | 改用 `unittest discover` |
| implementation | 备份数据库通过 read-only URI 执行 `quick_check` 时受 WAL/打开方式影响 | 初始恢复测试失败 | 旧 `_sqlite_integrity` 实现 | 改为普通打开后 `PRAGMA query_only=ON`；4/4 通过 |
| regression | Skill 夹具已是 16 case / 15 risk，旧 Agent 测试仍断言 14/14 | Agent 全套首次 1 项失败 | 系统 Python 运行 `tests/agent/` | 更新过期断言；35/35 通过 |
| environment | 系统 `/usr/bin/python3` 为 3.9.6，既有 Sync 包使用 Python 3.10+ 类型语法 | 首次完整工作区停在 Sync import | `python3 scripts/validation/run_workspace_validation.py` | 使用隔离 Python 3.12.13 venv；14/14 通过 |
| environment | Python 3.12 受 PEP 668 管理，拒绝全局安装依赖 | 首次 3.12 依赖安装被拒绝 | `python3.12 -m pip install -r scripts/requirements-dev.txt` | 不使用 `--break-system-packages`；改用 `/tmp` venv |
| repository | 通用 `coverage/` ignore 误伤 `research/coverage/` | 研究正本不会进入变更集 | `git check-ignore -v research/coverage/_INDEX.md` | `.gitignore` 增加精确 research 例外 |
| external | 无真实参与者、首发地区/平台/渠道与量化抽样框 | 不能给覆盖率、授权率和规模结论 | 运行 7 天 Pilot | 产品/研究 Gate |
| external | Reference DB 为明文 SQLite，Raw key 外部提供 | 不证明移动生产备份、E2EE、账户恢复 | 物理设备灾难恢复演练 | 架构/安全/移动 Gate |
| external | 未运行物理设备、真实宿主、真实来源与旁观者场景 | 不证明生产无感获取或真实复用 | Pilot + 真机矩阵 | 产品/发布 Gate |

## Privacy and evidence handling

- 是否只用 synthetic/测试账户：是；无账户，全部 synthetic fixture。
- 是否包含 Restricted/真实参与者数据：否。
- 原始证据保存位置、访问人、删除时间：无真实原始证据；临时数据库、Raw ciphertext、备份和 venv 在测试临时目录或 `/tmp`，不提交仓库。
- 仓库中只保留哪些脱敏指标/截图/hash：仅合成夹具、机器契约、测试断言和无正文聚合计数；无本轮截图。

## Review decision

- Continue / narrow / rerun / reject：`continue` T0 Pilot；P1/P2 来源扩张保持窄范围；P3 持续采集继续 hold。
- Reopened requirements/decisions：选择生产恢复路线（用户自有存储或 E2EE 云备份）；确定首发地区/平台/渠道；确认 7 天研究招募与删除安排。
- Next Gate input：T0 8 人 × 7 天有效样本、真实来源漏斗与重要遗漏、至少一次真实复用、双端物理恢复/删除演练、真实宿主与来源权限证据。
