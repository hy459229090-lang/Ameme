#if os(macOS)
import SwiftUI

@main
struct AmemeAppMacStub: App {
    var body: some Scene {
        WindowGroup {
            Text("Ameme iOS App requires an iOS 18 target.")
                .padding()
        }
    }
}
#endif
